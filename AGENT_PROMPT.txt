You are coding inside a repo that must implement a highly secure Docker Compose sandbox for OpenClaw on macOS Docker Desktop (Apple Silicon).

Read AI_CONTEXT.md first. Then generate the required files exactly:
- docker-compose.yml (commented, security-first)
- squid.conf (Slack-only whitelist)
- firewall.sh (iptables lockdown)
- security-test.sh (audits user, filesystem, routing, TCP/UDP/ICMP/DNS, proxy-only slack access)
- README.md with step-by-step commands to run and validate

Constraints:
- Prioritize security over simplicity.
- openclaw container must have no internet route (internal network only) and default OUTPUT DROP in iptables.
- Only squid has egress and only to whitelisted slack domains via CONNECT 443.
- Use cap_drop ALL and only add NET_ADMIN if needed; keep no-new-privileges, read-only root, tmpfs /tmp, non-root runtime.